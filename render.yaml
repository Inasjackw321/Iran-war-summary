services:
  - type: web
    name: iran-summary
    runtime: docker
    plan: free
    healthCheckPath: /health
    # Set these in the Render dashboard under Environment:
    # TELEGRAM_API_ID, TELEGRAM_API_HASH, TELEGRAM_PHONE,
    # GEMINI_API_KEY, FLASK_SECRET_KEY, TELEGRAM_SESSION (after first login)
